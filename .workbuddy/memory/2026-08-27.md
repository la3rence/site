# 2026-08-27

## Go (围棋) 对弈页：9x9 切换 + 棋谱持久化

- 重构 `lib/go-rules.js`：去掉硬编码 `N=19`，所有纯函数改为按棋盘尺寸 `N` 参数化，导出 `BOARD_SIZES=[9,13,19]` 与 `starPoints(N)`（9→[2,4,6], 13→[3,6,9], 19→[3,9,15]）。
- `components/katago-board.js`：新增 `size` 状态（默认 19 保持原行为）与尺寸选择器（9×9 / 13×13 / 19×19）；`changeSize()` 切换即开新局；`analyze` 用 `sizeRef.current` 驱动 `boardXSize/boardYSize`，避免切换瞬间闭包拿到旧尺寸。
- 棋谱持久化：将 `{size, userColor, moves, phase, analysis, result}` 写入 `localStorage["go-game-state-v1"]`；mount 时水合恢复（SSR 安全：初始渲染用默认空局，mount 后恢复），若恢复时仍处于 AI 思考阶段则自动续跑 AI。
- i18n：`lib/locales/{zh,en}.json` 新增 `Board size`（棋盘尺寸）。
- 验证：oxlint 0 errors；go-rules 功能测试 9/19 路均通过（81/361 cells、坐标往返、合法性判断正确）。

## Go 页状态栏微调

- 状态栏改为对称三栏 flex 布局：左=状态文字（`text-left` + `truncate`），中=思考动画（`shrink-0` 居中），右=提子/手数（`text-right` 右对齐）。AI 思考时加载动画居中，手数右对齐。
- 修正：状态栏未加宽度约束、与棋盘 `mx-auto max-w-180` 不匹配，导致左右两端贴到页面边缘。已给状态栏加 `mx-auto max-w-180`，使其与棋盘同宽居中，轮次/手数遂落在棋盘上方左右两侧，思考动画仍居中于棋盘。
- 状态栏左右两组文字各加 `mx-2` 内边距，不再贴棋盘边缘。

## Go 页：开新局/认输/切盘 二次确认弹窗

- 新增内置模态弹窗（非 `window.confirm`，zinc 灰阶、无蓝色，匹配棋盘视觉），避免误操作丢失当前对局。
- `dialog` state + `askConfirm(title, message, onConfirm, confirmText)` / `runConfirm()`，ESC 或点遮罩关闭。
- 触发点：换执黑/白（`newGame`）、切换棋盘尺寸（`changeSize`，同尺寸跳过）、认输（`resign`）、认输后「再来一局」（`newGame(userColor)`）。
- i18n 新增：`Confirm`/`Cancel`/`Start a new game?`/`Resign this game?`/`Switch to {size}×{size} board?`/`Current game progress will be lost.`（中英文补齐）。
- 胜率条标签 `AI` → `AI 胜率`（i18n `AI winrate`，槽位 `w-7`→`w-16`）。
- 棋盘上下外边距：状态栏 `mb-4`→`mb-8`，分析面板与操作区 `mt-4`→`mt-8`。

## Go 页：localStorage 按棋盘尺寸分桶存储

- 由单一扁平 key `go-game-state-v1`（含 `size` 字段、切换即覆盖）改为**按尺寸分桶容器**：`{ "9":{...}, "13":{...}, "19":{...}, _lastSize: 19 }`。
- `loadSaved(size)` 读指定尺寸；`saveStateForSize(size, state)` 写入并记 `_lastSize`；持久化 effect 按当前 `size` 分桶写。
- `changeSize()` 切换时不再清空目标尺寸，而是 `loadSaved(newSize)` 恢复该尺寸自己的棋局（含思考中续跑 AI）；无存档才开新局。
- 水合时按 `_lastSize` 恢复（默认 19）。
- 兼容旧版扁平存档：水合时检测 `size+moves` 且无数字分桶键 → 整体覆盖为分桶结构迁移，避免丢失进行中对局。
- 抽公共 `resumeIfThinking(saved)` 处理「思考中刷新/切换 → 续跑 AI」。
- 验证：oxlint 0 errors；node 模拟 localStorage 单测通过（分桶读写、13 路返回 null、旧档迁移）。
